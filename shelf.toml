name = "threads-mojo"
version = "0.3.0"
description = "Minimal pthreads for Mojo: spawn/join, atomics, a mutex, parallel_for, and a WorkerPool"
tags = ["magmalake", "threads", "pthreads", "concurrency", "parallel", "atomics", "worker-pool"]
# other tins this one depends on; omit if none
tins = []
